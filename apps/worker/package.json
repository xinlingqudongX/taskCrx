{
    "name": "@team-session/worker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "deploy:api": "tsx deploy.ts",
        "test": "vitest --run"
    },
    "dependencies": {
        "@team-session/shared": "workspace:*",
        "cloudflare": "^6.3.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240512.0",
        "@types/node": "^24.12.4",
        "tsx": "^4.22.3",
        "typescript": "^5.5.0",
        "vitest": "^1.0.0",
        "wrangler": "^4.95.0"
    }
}
